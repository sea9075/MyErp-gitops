apiVersion: v2
name: myerp
description: >-
  MyERP backend-api / worker 部署用的 Helm chart（GitOps 模式：ArgoCD 監看這個 repo，
  一有變更（例如 CI 更新 image tag）就自動 sync 到叢集，不需要手動 helm install/upgrade，
  見 Infra-Progress.md §32、Hybrid-Cloud.md §8/§9）。
type: application
version: 0.1.0
appVersion: "1.0.0"
